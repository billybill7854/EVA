fastapi==0.115.6
uvicorn==0.34.0
python-telegram-bot==21.10
python-dotenv==1.1.0
pydantic==2.13.4
pydantic-settings==2.7.0
sqlalchemy==2.0.41
aiofiles==24.1.0
httpx==0.28.1
aiohttp==3.14.1
python-dateutil==2.9.0
pyyaml==6.0.2
requests==2.32.3

# Database and Cache
redis==5.2.1
aiosqlite==0.20.0

# Vector Database (optional — comment out if not needed)
pinecone==5.0.1

# Google APIs
google-api-python-client==2.166.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# Payment Processing
stripe==12.1.0

# Additional utilities
cryptography==44.0.3

# Telegram user account (MTProto)
pyrogram==2.0.106
# tgcrypto speeds up Pyrogram encryption but requires Microsoft C++ Build Tools to compile.
# Install separately if you have VS Build Tools: pip install tgcrypto
# Without it Pyrogram still works, just slower on encryption.